generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model User {
  id       String   @id
  email    String
  name     String?
  mission  String?
  tags     Json?
  createdAt DateTime @default(now())
}
